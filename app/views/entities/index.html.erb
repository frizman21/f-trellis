<% content_for :title, "Entities" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="mb-0">Entities</h1>
  <%= link_to "New entity", new_entity_path, class: "btn btn-primary" %>
</div>

<% if @entities.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Entity</th>
        <th>Type</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @entities.each do |entity| %>
        <tr>
          <td><%= link_to entity.label, entity_path(entity) %></td>
          <td><%= link_to entity.entity_type.name, entity_type_path(entity.entity_type) %></td>
          <td><%= link_to "Edit", edit_entity_path(entity), class: "btn btn-sm btn-outline-primary" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= paginate @entities %>
<% else %>
  <p>No entities yet.</p>
<% end %>
