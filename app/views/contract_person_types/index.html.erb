<% content_for :title, "Contract ↔ Person Types" %>

<div class="d-flex justify-content-between align-items-center mb-3">
  <h1 class="mb-0">Contract ↔ Person Types</h1>
  <%= link_to "New relationship type", new_contract_person_type_path, class: "btn btn-primary" %>
</div>

<% if @contract_person_types.any? %>
  <table class="table">
    <thead><tr><th>Name</th><th>Description</th><th>Additional attribute keys</th><th></th></tr></thead>
    <tbody>
      <% @contract_person_types.each do |type| %>
        <tr>
          <td><%= type.name %></td>
          <td><%= type.description %></td>
          <td><%= type.additional_attribute_keys.join(", ") %></td>
          <td><%= link_to "Edit", edit_contract_person_type_path(type), class: "btn btn-sm btn-outline-primary" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>No relationship types defined.</p>
<% end %>
