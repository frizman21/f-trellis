<% content_for :title, "Edit #{@entity_type.name}" %>
<%= render "projects/header", project: @project, side: :ontology, section: :entity_types %>
<h1>Edit <%= @entity_type.name %></h1>

<%= render "form", project: @project, entity_type: @entity_type, submit_label: "Update entity type" %>
