<% content_for :title, "New Entity Type" %>
<%= render "projects/header", project: @project, side: :ontology %>
<h1>New Entity Type</h1>

<%= render "form", project: @project, entity_type: @entity_type, submit_label: "Create entity type" %>
