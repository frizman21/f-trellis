<% content_for :title, "Edit #{@evaluation.name}" %>

<h1 class="mb-3">Edit <%= @evaluation.name %></h1>

<%= render "form", evaluation: @evaluation, skills: @skills, models: @models,
                   learning_sets: @learning_sets, submit_label: "Save evaluation" %>
