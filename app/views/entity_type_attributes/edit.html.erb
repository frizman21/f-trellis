<% content_for :title, "Edit Attribute" %>
<%= render "projects/back", project: @project, side: :ontology %>
<h1>Edit <%= @attribute.name %></h1>
<p class="text-muted">On <%= link_to @entity_type.name, project_entity_type_path(@project, @entity_type) %></p>

<%= render "form", project: @project, entity_type: @entity_type, attribute: @attribute, submit_label: "Update attribute" %>
