powers:
  project: apollo
  name: Powers
  description: The engine provides thrust for the vehicle.
  from_entity_type: rocket_engine
  to_entity_type: launch_vehicle

# No attributes, so the relationship edit page's empty state is reachable.
bare_relation:
  project: apollo
  name: Bare Relation
  from_entity_type: rocket_engine
  to_entity_type: bare

# Both ends the same type, which is legitimate — one engine superseding another.
supersedes:
  project: apollo
  name: Supersedes
  from_entity_type: rocket_engine
  to_entity_type: rocket_engine

# Another project's, so scoping can be asserted as isolation.
gemini_docks:
  project: gemini
  name: Docks With
  from_entity_type: gemini_capsule
  to_entity_type: gemini_capsule
