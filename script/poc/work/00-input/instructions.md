<!--
Generated by script/poc/bin/export-model from project 6 "Extraction PoC"
at 2026-08-20T16:11:57Z, via ExtractionPrompt.

llm-process sends everything below this comment as the model's instructions,
verbatim. Edit it freely — trying a different prompt against the same sources
is the point. export-model will not overwrite your edits without --force.

If you change what the reply must look like, change mental-model.json's schema
too, or the validator will hold the model to a contract it was never given.
-->

You are reading a source document and extracting the things it describes for
the "Extraction PoC" knowledge base.

Read the content that follows the instructions. Identify every entity and
every relationship it describes that matches one of the definitions below.
Populate the JSON structure at the end of these instructions and return
only that JSON — no explanation, no commentary, no markdown fence.

Rules:

- Record only what the source states. If the source does not give a value
  for an attribute, leave that attribute out of the list. Do not infer,
  estimate, or fill a gap from your own knowledge.
- Attributes are a list of name and value pairs, and every entity and
  relationship carries one. Where the source states nothing, the list is
  empty — the key is still there.
- Every entity needs an id, a name, and a type. The name is what the source
  calls the thing.
- Give each entity a short id of your own — e1, e2, org-nasa, whatever is
  readable — unique within this response. These ids are yours: they mean
  nothing outside this reply and are not database identifiers.
- Refer to entities in relationships by those ids, never by name. Names
  repeat and vary between sentences; an id you assigned does not.
- A relationship may only join the entity types its definition names, in
  the direction it names.
- If the source describes nothing that matches a definition, return the
  structure with empty lists.

Entity types:

- Contract: A specific funded agreement between parties, identified as one thing by the source — an award, a task order, a grant. A programme that funds many awards is not itself a Contract unless the source names it as a single agreement.
    - award_amount (float), optional
    - contract_number (string), optional
    - end_date (datetime), optional
    - start_date (datetime), optional

- Organization: A company, agency, university, laboratory, or other body that acts as one. A division named only in passing is part of its parent, not its own Organization, unless the source treats it as acting on its own behalf.
    - employee_count (int), optional
    - founded (int), optional
    - headquarters (string), optional
    - website (string), optional

- Person: An individual human being named by the source. A role held by nobody in particular is not a Person, and neither is a team, a department, or a job title with no name attached to it.
    - email (string), optional
    - phone (string), optional
    - title (string), optional

- Product: A specific thing offered for sale or delivery under its own name — a model, a platform, a system. Distinct from Technology: a Product is the article, the Technology is what makes it work.
    - category (string), optional
    - model_number (string), optional
    - price (float), optional
    - released (datetime), optional

- Science: A field of study or a body of underlying principle — fluid dynamics, materials science, machine learning theory. Distinct from Technology: Science is what is understood, Technology is what is built with it.
    - discipline (string), optional
    - field (string), optional
    - key_publication (string), optional

- Technology: A capability or technical approach that can be built into things — a propulsion method, a sensing technique, an architecture. Not a product you can buy, which is a Product, and not the underlying principle it rests on, which is a Science.
    - application_area (string), optional
    - category (string), optional
    - maturity_level (string), optional

Relationship types:

- Acquires: The first organization acquires the second, taking ownership of it. The direction is acquirer to acquired. An investment, a partnership, a supplier arrangement, or a merger of equals is not an acquisition.
    Joins an Organization (from) to an Organization (to).
    - announced (datetime), optional

- Contract Develops: The contract funds work that builds or advances this technology. Naming a technology as background is not developing it.
    Joins a Contract (from) to a Technology (to).

- Contract Researches: The contract funds study in this field.
    Joins a Contract (from) to a Science (to).

- Develops: The organization builds or advances this technology itself. Buying or using it is not developing it.
    Joins an Organization (from) to a Technology (to).

- Employs: The organization employs this person, or the person holds a named position at it. A person merely quoted about an organization is not employed by it.
    Joins an Organization (from) to a Person (to).
    - role (string), optional

- Enters: The organization is a party to this contract — it was awarded it, signed it, or is named as performing it. An agency that awarded the contract is also a party and belongs here.
    Joins an Organization (from) to a Contract (to).
    - role (string), optional

- Point Of Contact: The person the contract names as a contact — principal investigator, technical contact, contracting officer. Somebody mentioned in the same paragraph is not a point of contact.
    Joins a Contract (from) to a Person (to).
    - role (string), optional

- Researches: The organization conducts study in this field. Applying an established result is not researching it.
    Joins an Organization (from) to a Science (to).

- Sells: The organization offers this product commercially. A reseller or distributor counts; a customer does not.
    Joins an Organization (from) to a Product (to).

- Supports: This field of study is the principle the technology rests on — the source states the technology works because of it.
    Joins a Science (from) to a Technology (to).

- Uses: The product is built with this technology, as the source describes it — a specification, a feature, a stated approach.
    Joins a Product (from) to a Technology (to).

The JSON structure to return:

{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "entities",
    "relationships"
  ],
  "properties": {
    "entities": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "id",
          "name",
          "type",
          "attributes"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "A short id you assign, unique within this response."
          },
          "name": {
            "type": "string",
            "description": "What the source calls this thing."
          },
          "type": {
            "type": "string",
            "enum": [
              "Contract",
              "Organization",
              "Person",
              "Product",
              "Science",
              "Technology"
            ]
          },
          "attributes": {
            "type": "array",
            "description": "The attributes this entity has, as name and value. Use the attribute names given in the definitions above, and include only the ones the source states.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "name",
                "value"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "enum": [
                    "application_area",
                    "award_amount",
                    "category",
                    "contract_number",
                    "discipline",
                    "email",
                    "employee_count",
                    "end_date",
                    "field",
                    "founded",
                    "headquarters",
                    "key_publication",
                    "maturity_level",
                    "model_number",
                    "phone",
                    "price",
                    "released",
                    "start_date",
                    "title",
                    "website"
                  ]
                },
                "value": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    },
    "relationships": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "type",
          "from",
          "to",
          "attributes"
        ],
        "properties": {
          "type": {
            "type": "string",
            "enum": [
              "Acquires",
              "Contract Develops",
              "Contract Researches",
              "Develops",
              "Employs",
              "Enters",
              "Point Of Contact",
              "Researches",
              "Sells",
              "Supports",
              "Uses"
            ]
          },
          "from": {
            "type": "string",
            "description": "The id of the entity at the from end."
          },
          "to": {
            "type": "string",
            "description": "The id of the entity at the to end."
          },
          "attributes": {
            "type": "array",
            "description": "The attributes this relationship has, as name and value. Use the attribute names given in the definitions above, and include only the ones the source states.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": [
                "name",
                "value"
              ],
              "properties": {
                "name": {
                  "type": "string",
                  "enum": [
                    "announced",
                    "role"
                  ]
                },
                "value": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    }
  }
}

An example of a correct reply, using this project's own types:

{
  "entities": [
    {
      "id": "e1",
      "name": "The name the source gives this Organization",
      "type": "Organization",
      "attributes": [
        {
          "name": "employee_count",
          "value": "1"
        },
        {
          "name": "founded",
          "value": "1"
        },
        {
          "name": "headquarters",
          "value": "the headquarters the source states"
        }
      ]
    },
    {
      "id": "e2",
      "name": "The name the source gives this Organization",
      "type": "Organization",
      "attributes": [
        {
          "name": "employee_count",
          "value": "1"
        },
        {
          "name": "founded",
          "value": "1"
        },
        {
          "name": "headquarters",
          "value": "the headquarters the source states"
        }
      ]
    }
  ],
  "relationships": [
    {
      "type": "Acquires",
      "from": "e1",
      "to": "e2",
      "attributes": [
        {
          "name": "announced",
          "value": "1969-07-16T13:32:00Z"
        }
      ]
    }
  ]
}
